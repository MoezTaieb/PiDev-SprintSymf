{% extends 'baseBack.html.twig' %}

{% block body %}
    <h4>Categorie equipment creation :</h4>
    </div>
    <div class="form-body" data-example-id="simple-form-inline">

            <div class="form-group">
                {{ form_start(form) }}
                {{ form_widget(form) }}

                {{ form_end(form) }}


            </div> <button type="submit" class="btn btn-default">create</button>
            <a href="{{ path('categorieequipment_index') }}">Back to the list</a>

    </div>
    </div>
{% endblock %}
